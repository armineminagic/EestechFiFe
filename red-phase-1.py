import sys
lista = {"kljuc":1,}

class Kolekcija:
    kljuc = {"key"}
    vrijednost = {1}

    def add(self):
        if kljuc in self.kljuc:
            vrijednost += self.vrijednost
        else:
            kljuc+=key
            vrijednost += 1


for line in sys.stdin:
    (key,value) = line.split(',')
    for item in lista:
        if key in lista:
            lista[key]= value + 1
            pronadjen = true

print lista